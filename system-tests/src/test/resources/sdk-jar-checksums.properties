# SHA-256 of the odds-feed jar each sdk.version is expected to resolve to.
#
# The repository order in system-tests/pom.xml asks Central before the packages registry, for
# every coordinate including this one. That is safe only while nobody else can publish
# com.oddin.oddsfeed there. This pin makes the question moot: whichever repository answers, a
# jar that is not the one we know fails the build.
#
# Each digest was taken from the artifact the packages registry serves and checked against the
# jar attached to the matching GitHub release - identical contents, different zip metadata,
# because the 0.0.x Gradle build is not reproducible.
0.0.56-rc2=39e271ddf49ac2795178a507420d6e5fb6de5261deb4f4b96b775c6cee298569
# 0.0.54 has no release asset to check against, so this one comes from the registry alone.
0.0.54=432fc5e6e0fe8b90f8baa237d129f3931f05d2fcf3089df2e49f4b9adfabe272
